import MultiTable from "./components/multi-table.jsx";

const Ppapp = () => {
    //If User not auth'd, redirect to home
    // Have a user icon (top right) to logout
    //Mutlitable
    return (
        <MultiTable />
    )
}

export default Ppapp;